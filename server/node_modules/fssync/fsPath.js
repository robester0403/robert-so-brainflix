'use strict';
const fsPath = require('path');
const kit = require('./kit');

module.exports = Object.assign(kit, fsPath, {
	cwd: kit.toUnixPath(process.cwd()),
	resolvePath: fsPath.resolve,
	isAbsolutePath: fsPath.isAbsolute,
	dirname: (path)=> kit.toUnixPath(fsPath.dirname(path), 1),
});


